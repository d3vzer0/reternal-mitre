name: '[T1033] System Owner/User Discovery - Network'
integration: Splunk
reference: https://github.com/olafhartong/ThreatHunting
description: Hunting query based on sysmon or windows events for Splunk
author: olafhartong
mitre_technique:
  id: T1033
  datasources:
  - Process use of network
coverage:
  app:
    and: ThreatHunting
  sourcetype:
    and:
    - XmlWinEventLog:Microsoft-Windows-Sysmon/Operational
search: '`indextime` `sysmon` event_id=3 (dest_port=389 OR dest_port=636 OR dest_port=445
  OR dest_port=8080) \
  | transaction process_guid maxspan=600s \
  | eval target_hosts=mvcount(dest_ip) \
  | where target_hosts>5 \
  | eval mitre_technique_id="T1069" \
  | `network_whitelist`\
  | eval indextime = _indextime | convert ctime(indextime) | table _time indextime
  event_description host_fqdn user_name process_path process_id process_parent_id
  process_command_line process_guid src_ip dst_ip dst_port src_host_name dst_host_name'
